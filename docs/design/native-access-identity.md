# Native account identity and read boundary

Status: isolated source candidate; private activation disabled. This design applies only to `account/`, a separate generic Worker, not the existing installation-mint runtime.

Entry recovery is advisory and read-only in the existing authority. Exact live, terminal/absence, verification and revoked/unknown states select fixed routes or truthful recovery HTML without cookie/storage mutation. Actual begin independently resolves authoritative ledger/session identity and current epoch, including trusted expiry and missing session cookies, so transient route reads cannot create anonymous Pending for a retained identity. A supplied missing record, dangling active pointer, malformed epoch/session or browser disagreement denies. Same-ID/current-epoch binding remains mandatory at final activation; expiry does not create usable authority. Explicit existing signout is offered only for its supplied record's eligible proof and truthfully may end a replacement that arrives before submission; deleted-proof replay still denies. Unknown errors do not falsely retire a live continuation. The global uncertain-start marker remains unaffected and visible. No new session, grant, signout or provider protocol is introduced.

Repository start admission and its actual single GrantDO Pending write now share the browser authority event gate through exact state attachment. Assertion/bootstrap is outside the gate to avoid authority re-entry; GitHubOAuth.start performs local storage/PKCE/URL construction only. Both account and connector starts persist one encrypted global attempt marker before dispatch. Strict undefined-only absence, complete attempt matching and bounded recognition of the existing successful GitHub authorization URL response govern clearing. An unrecognized response, read error or transport loss retains uncertainty across restart, Cancel, signout and lease expiry, blocking all new starts across browser handles indefinitely until separately verified reconciliation. There is no automatic recovery, marker-clear endpoint or distributed rollback claim. Boolean-only status suppresses new Connect and truthfully explains global unavailability without exposing subject/browser/lease bindings. Existing grants and public access remain separate. A recognized completed write followed by failed attachment returns no URL; its lease cannot replay the attempted write.

Dashboard repository authorization uses one discriminated encrypted repositoryState lease. Explicit account-purpose preparation captures authenticated session CSRF, identity/epoch and terminal-or-absence slot/revision before broker start. Connector-purpose start likewise atomically reserves its exact ref-bound operation before broker dispatch, then attaches only the returned state to that reservation. Cross-purpose and duplicate live writers deny before CSRF/provider mutation. Callback consumption retains an account exchanging record; the existing browser final gate binds its exact candidate while marking committing before internal vault I/O. Success removes only that lease; uncertain outcomes do not imply rollback. All identity begin modes and final activation guard both repository purposes through the authoritative active ledger, including reversed provider ordering. Exact account Cancel works only before committing; connector Cancel releases only matching connector repositoryState. Existing grant candidate protocol, provider scopes and connector state/S256 contracts are unchanged.

Standalone terminal-flow recovery is explicit and local: `/account/signin?restart=standalone`. The encrypted browser authority accepts only an absent slot or the exact terminal slot, preserves trusted expected identity/current epoch, and fails closed on malformed storage. Its persistent safe-integer slot revision is distinct from session generation and survives slot deletion, preventing replacement/retirement ABA. Pending and retained recognized state bind mode, ref hash, nonce, browser, generation, snapshot and revision; checks precede start, consume, activation and exact cancellation. Ordinary no-ref login has a separate true-absence snapshot and cannot overwrite a live continuation. Cancel clears only the matching ledger nonce and Pending, so an already consumed activation fails its final nonce check. No shared continuation-cookie clearing or old connector-intent resumption occurs. The exact local source fire and validation boundaries are recorded in the source return.

A configured central-account issuer authenticates a stable subject and verified numeric GitHub ID. A distinct service issuer authenticates the calling service. Both asymmetric ES256 assertions must have the exact audience, resource and service. The broker never accepts decode-only JWTs or a service credential as authority to choose a person's grant.

GitHub classic OAuth `repo` is broad upstream authority, including writes across accessible repositories. This service exposes only five read actions; calling it read-only does not narrow the GitHub grant. Repository code grants must return exactly the expected normalized `repo` scope both at the token response and `/user` scope header, with positive access/refresh expiry and a rotating refresh token. Identity-only sign-in requires exactly `read:user` and cannot persist a repository grant. Any other behavior denies the affected operation. Actual GitHub.com behavior is a separate unrun provider gate, not established by intercepted responses.

The maintained OAuth4WebAPI adapter owns state/PKCE/code/refresh protocol operations. Subject-sharded Durable Objects retain one pending expiring transaction and a JWE encrypted credential envelope. JOSE A256GCM binds subject and generation inside the encrypted envelope. Atomic compare-and-swap handles rotation; a process's single-flight prevents concurrent refresh replay. An uncertain refresh state cannot reuse its old token. Disconnect wins against stale refresh completion.

Grant `generation` changes on credential rotation; `epoch` changes on grant replacement or revocation. Renewal can advance assertions only inside their original epoch. This distinction prevents a retired connector token from reconnecting itself while allowing legitimate refresh lineage. Signed account assertions from the owned account are required for a new consent epoch.

`BrokerReadRequest` has request ID, exact resource/action, owner/name, optional canonical ID only for `resolve_repository`, and optional ref/SHA/path. Every operation performs fresh authorized provider repository metadata lookup before data access; supplied IDs must match. Default-branch resolution obtains a full commit SHA, including non-main branches. Subsequent tree/blob/archive operations are pinned to that snapshot. No arbitrary URL, method, headers or unknown request keys are accepted. All other actions require numeric repository ID.

The response binds subject, numeric GitHub ID, service, exact resource/action, canonical repository ID, generation, decision/request IDs, metadata-check time, snapshot SHA and exact intent. A generation check immediately before response suppresses disconnect races. No positive authorization cache exists. Immutable snapshot identity is evidence, not future authorization.

Archive redirects are limited to the exact owner/repository/commit codeload path derived from the authorized request. Provider Authorization never crosses origins. Streaming size limits bound compressed data; downstream archive expansion/path extraction and every persisted disclosure remain separate consumer obligations. Blob responses must be files at the exact requested path, with base64 content and full blob SHA; tree truncation/path traversal denies. Credential echoes in wire data, including base64 payloads, deny.

The native connector uses Workers OAuth Provider 0.7.2 with exact resource matching and safe props. `/connector/session` is a fixed transport alias internally rewritten to the configured resource for the provider's actual request-URL audience check; no remote fetch or arbitrary routing results. That protected handler issues a current-generation short assertion only after separate service verification. Registered-client consent is explicit and authenticated; no new dynamic registration endpoint is enabled.

Synthetic tests cover two users and three owner-qualified same-name repositories, scope inheritance, callbacks/identity/PKCE/state replay, refresh races, revocation, confused deputy inputs, no credential payload, actual maintained native OAuth token exchange and resource rejection. Live account UI, provider, cloud persistence and consumer caching remain separate activation/validation gates.

Learning: (1) a single monotonically increasing generation cannot distinguish token refresh from reconnect—use and test consent lineage; (2) a protocol helper passing in isolation cannot prove a native opaque-token chain—exercise actual provider token and request-URL semantics; (3) cold owner/name discovery must derive numeric IDs rather than require IDs that only fixture authors already know.

## Approved v1.5 account authority

The account service now owns immutable GitHub numeric identity and local browser sessions for this meal. Supabase runtime/session custody is removed; this is an explicit maintenance transfer, not a claim managed authentication was built. The account remains unconfigured and disabled.

The identity-only bootstrap is a separate typed seam and never weakens SessionContext for repository OAuth. Maintained oauth4webapi performs S256/state/code exchange against fixed GitHub endpoints. Exact read:user evidence is required both in token response and fresh numeric /user verification. Both identity tokens remain request-local and are discarded, never browser/stored/model values. Five-minute encrypted pending transactions bind state, CSRF and browser; atomic consumption precedes provider exchange.

One account-authority-v2 Durable Object owns both identity indexes, encrypted token-free sessions, pending/activation leases, browser generations, per-account epochs and revocation sequence. Atomic activation resolves the immutable mapping and verifies expected reauthentication identity plus final browser/account epochs. Account-specific revoke sequence rejects callbacks started before that account's revoke, including anonymous bootstrap; unrelated revocations do not deny them. A new begin replaces the pending nonce without retiring the currently valid session. Successful reauthentication rotates the handle and preserves a minimal encrypted signout tombstone for the prior handle.

Opaque session handles are hashed indexes. V2 sessions enforce30-minute idle/eight-hour absolute bounds, no managed refresh, five-minute freshness for NEW repository/connector consent. Activity touch never advances fresh-auth time. Signing retains final load/epoch checks. Repository provider completion instead stages one encrypted, unusable candidate per subject; final browser-authority event gating validates exact browser generation/account epoch and five-minute freshness before internal candidate consume/grant CAS. Candidate binds subject/numeric ID/expected grant generation/status and random one-use ID, with five-minute post-preparation expiry. NEW connector code/grant persistence is also inside that browser gate through maintained getOAuthApi, not merely followed by response suppression. Provider I/O remains outside the gate. Revoke-all-local-browser-sessions is intentionally separate from already issued connector sessions and grant disconnect. GitHub identity revocation is not continuously polled after credential disposal.

The browser event gate is a linearization boundary, not a cross-DO transaction: commit-first may leave an established grant after later local signout, as specified. Timeout/transport loss may leave the commit outcome unknown; no rollback or 'no access granted' claim follows an uncertain response. The platform's30-second event-gate reset and maintained-provider partial KV failure recovery remain operational/native validation gates. Public raw OAuth and cookie-less connector POST routes deny even valid existing connector assertions. Trusted internal substrate fixtures are separately labeled and cannot stand in for the public browser journey.

Browser-only continuation now preserves a validated original connector authorization through sign-in, same-principal reauthentication and separate repository consent. One encrypted slot per browser binds an opaque-ref hash, exact client/state/registered redirect/S256/resource/read scope, browser generation, expected immutable identity/account epoch and an absolute five-minute deadline. Intent is at most7KiB and the serialized plaintext record at most8KiB in UTF-8. Admission limits10creations/browser/minute and1000global/minute before new ledger allocation; expiry denies even before operator cleanup. Each form binds its exact ref and CSRF, never a shared cookie's replacement slot. Pending identity transactions capture ref/nonce/generation; bounded retained state hashes distinguish old callbacks before they can burn replacement Pending state. Up to60live state bindings/browser are retained for the five-minute callback window; capacity denies, never evicts live bindings.

Expired-session recovery derives expected identity only from the still-valid trusted continuation/ledger/current epoch, never from decoded expired tokens or caller identity. Wrong identity retires the captured continuation only; signout/revoke/expiry invalidate it. Explicit cancellation is available before identity, during repository consent and at connector consent. Fixed /account/continue selects the next internal stage and preserves original client OAuth fields. Final commit spends and removes intent before maintained completion under the browser gate; failed/uncertain completion remains spent, with no authorization-code response replay. Expired/spent/replaced requests receive terminal restart-from-connector guidance rather than an unusable sign-in loop.

This browser continuation does not preserve or resume a failed Cartographer MCP tool request or selected snapshot. That separately owned consumer continuation and actual supported-host correlation remain open. No DCR, namespace/configuration, credentials, live grants or deployment is added here. The public CTA is a fixed link to the documented Cartographer public explorer, not a caller-controlled destination; no actual-host availability check is claimed by a local link test.

The old registry/managed-session namespaces are not production verification authorities and are not automatically migrated. Existing grant namespaces/generations remain untouched. Activation requires persisted-subject inventory and accepted restore consistency; a new empty namespace proves nothing about legacy subjects. Key purpose/version rotation, bounded overlap, restore invalidation, conservative login-start rate limits, redacted errors and operator ownership are specified in account/README.md. Missing production custody/retention/alert ownership remains a gate.

Repository authorization requests repo offline_access, with returned exact repo/expiry evidence conservatively enforced. Actual returned offline_access semantics and shared-registration identity-scope isolation are unverified provider gates. No automatic extra registration, grant, configuration, DNS or deployment is implied.

Source validation distinguishes unit protocol checks, actual local SQLite/workerd lifecycle tests and rendered synthetic UI from actual GitHub/Cloudflare/client evidence. The latter, runtime parity, fresh independent review, Otto acceptance, required CI/Bugbot and release/learning closure remain open. Native public/private-cache contracts are unchanged.
