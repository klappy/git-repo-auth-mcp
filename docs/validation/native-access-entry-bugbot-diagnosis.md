# Otto — exact f688 Bugbot disposition

CURRENT END-TO-END DISPOSITION: external `eaa4aa` recovery is HOLD after the author's actual followed-navigation reproduction. The narrow response test below remains true, but its preliminary scope-safe interpretation does not establish safe destination behavior. Author executed injected load503 with valid identity1001/no continuation → dashboard303 → sign-in200/unbound Pending → real BrowserSessions start/consume/activate identity1002 without signout (four characterization tests reported PASS). This is an identity-binding defect, not proof of private grant/data disclosure. Otto has read the implicated exact route/store call chain; the full characterization execution belongs to the author. Wrapped-base64 FALSE POSITIVE disposition is unaffected.

Read-only review of local exact `f6889e7df5d1ddf11d7a1933d94a6ddaf65fe367`; remote `4903` equivalence is coordinator evidence, not independently fetched here. No product edits, source fire or live action.

## Report 3952845147: wrapped base64

FALSE POSITIVE for the reported wrapping behavior. Independently executed reviewer fixture `tests/native-access/otto-wrapped-base64-review.test.ts` in `/tmp/otto-standalone-4ad8-review`: 1 PASS, exit 0. Actual Miniflare/workerd (compatibility date 2026-06-16), bundled production `GitHubReads.read` and `AccountBroker.read`, synthetic upstream responses, external I/O denied. Ordinary 60-column CRLF-wrapped base64 plus trailing newline returned 200 and exactly decoded original contents. Wrapped access-token echo returned 403/access_denied through upstream checking; wrapped refresh-token echo returned 403/access_denied through broker's second decoding check. Thus this is not merely Node atob evidence, and retaining forgiving native decoding does not discard the tested credential-echo checks. Actual GitHub provider payload observation remains separate.

Reproducer SHA256: `621668f29cb2db8c2f2a18990f6a386f4a9067a5e53550383e4f188e613652c5`.

## Report 3952845140: stale session dashboard

Static defect confirmed in exact routes: a present stale session cookie bypasses the no-handle GET redirect, reaches unconditional browser load, and its failure reaches the outer catch. If a continuation cookie exists, that catch renders terminal wording solely from cookie presence, without proving the referenced slot is terminal; an otherwise live recoverable slot can therefore be falsely labeled unresumable. A generic 503 also obscures ordinary expiry recovery. Account author is independently executing the actual route/store reproduction; no native stale-session test is claimed by Otto in this receipt.

Minimal repair design constraints sent directly to author: do not convert a failed load into anonymous authority or silently begin/rebind/retire a flow. No shared-cookie clearing, storage-error-to-absence, new service or caller-controlled redirect. A read-only recovery response may offer existing fixed, server-guarded sign-in choices with neutral truthful wording; continuation availability must not be guessed from ambient cookies. Exact live ref recovery must preserve existing principal/resource intent and Pending/ref/epoch/replacement guards; sensitive POST/callback denials stay unchanged. A classification failure must remain unknown, not terminal or public authorization. Final written author design and source acceptance are separate; this review authorizes no implementation.

Current source's disabled integration acceptance is historical for its reviewed start-write scope; the new dashboard recovery defect remains open for functional completion. This does not reopen the false base64 finding or imply a provider/library change.

## Exact external eaa4aa navigation delta

Independently fetched `account/account-routes.ts` at `eaa4aa049809e47202f5ab3a6450f1ce91fb1da3`, Git blob `57d6410cd17f55a291b2cd5a55f06ec628fe651b`. Compared with f688: the sole substantive change is GET `/account` trying current load and, on failure, returning the existing fixed `/account/signin` 303 when enabled. No authentication authority is created, no `begin(noRef)` call occurs, no cookie is cleared, no private result is emitted and no slot is retired. The destination retains the existing strict server-owned begin/ref/principal/epoch guards. This navigation-only catch is not the unsafe authority fallback previously rejected.

Independently executed exact imported external route with injected ambiguous load 503 and stale session/browser/continuation cookies: 1 PASS, response 303/fixed sign-in, no Set-Cookie, only operation `load`, no false terminal text in this response, no broker call. Fixture `tests/native-access/otto-eaa-navigation-review.test.ts`, SHA256 `27f6772cb905da1499ef56d90f4c333c87a12927aae6bb4aabba32109d0e467f`. This is production-route/synthetic-binding evidence, not a native full identity recovery journey.

Superseded preliminary disposition: the isolated navigation delta performed no authority mutation; this was initially described as technically scope-safe subject to the full journey. The author's subsequent actual followed-navigation finding defeats end-to-end acceptance. The patch adds a duplicate valid-session load and does not eliminate the outer generic false-terminal wording if a later sign-in/classification fails. Never describe any catch as proof the slot is terminal. No source modification or integration permission is supplied by this review.

## Narrow repair design conditions sent to author

Existing begin transaction, for ordinary and explicit standalone modes, must derive any retained expected identity from authoritative ledger.active and the authenticated session record even when route active-load fails or its cookie is absent. A structurally valid expired identity may constrain reauthentication without becoming usable action authority: check same browser/key/generation, immutable numeric mapping and current account epoch. Malformed, unreadable, inconsistent, revoked or retired authority cannot silently become anonymous. A supplied inconsistent cookie cannot override authoritative ledger state. Anonymous initialization requires genuine absence and no bound continuation; explicit signout remains the account-switch boundary. Existing live repository lease exclusion, exact continuation/ref/revision, Pending and final identity/epoch checks remain mandatory. Route classification alone is insufficient because state can change before begin.

Unknown failures use neutral truthful text; do not assert a live request cannot resume. Existing fixed bound sign-in and explicit separate restart may be offered with accurate conditional descriptions, while the server decides whether each is allowed. Prefer neutral unknown copy over a new general classification framework. Require actual stale-session/live-continuation recovery and no-continuation same-ID recovery, transient-load-error with wrong-ID denial, missing-cookie authoritative ledger constraints, corrupt/revoked/retired negatives and concurrent replacement preservation. These are design conditions, not acceptance of an unwritten final amendment or authorization to implement.
