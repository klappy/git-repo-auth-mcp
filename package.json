{
  "name": "git-repo-auth-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Worker-minted, short-lived, scoped GitHub App installation tokens over MCP. Currently GitHub; forge-agnostic by design.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run deploy:staging",
    "deploy:staging": "node scripts/validate-deployment.mjs staging account/wrangler.staging.jsonc && wrangler deploy --config account/wrangler.staging.jsonc --experimental-autoconfig=false --keep-vars",
    "deploy:production": "node scripts/validate-deployment.mjs production wrangler.jsonc && node scripts/build-info.mjs && wrangler deploy --config wrangler.jsonc --experimental-autoconfig=false && node scripts/provision-service-key.mjs",
    "typecheck": "tsc --noEmit",
    "build-info": "node scripts/build-info.mjs",
    "test": "vitest run",
    "test:smoke": "vitest run test/smoke.live.test.ts",
    "test:account-host": "RUN_ACCOUNT_HOST=1 vitest run tests/native-access/account-host.test.ts"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "0.7.2",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@octokit/auth-app": "^8.2.0",
    "agents": "^0.15.0",
    "jose": "6.2.3",
    "oauth4webapi": "3.8.8",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  }
}
