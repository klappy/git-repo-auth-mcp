# Bugbot Teams Probe (disposable)

Purpose: a fresh, bot-authored PR to test whether a Cursor **Teams** Bugbot
account auto-runs Bugbot on **open** for a PR authored by `git-repo-auth[bot]`.

If the `Cursor Bugbot` check appears on this PR without any `bugbot run` comment
and without a close/reopen, then the account-type gate (Individual = own-PRs-only)
is removed under Teams. This file can be deleted once the result is recorded.
