export const accountStyles = 'body{font:18px system-ui;line-height:1.6;max-width:42rem;margin:auto;padding:2rem;color:#17212b;background:#f6f8fa}main{background:white;padding:2rem;border:1px solid #ccd3db;border-radius:1rem}button,a{font:inherit}button{padding:.6rem 1rem}a{color:#075bb5}form{margin:1rem 0}';
export const escape = (value: string) => value.replace(/[&<>"']/g, char => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char]!));
export function accountPage(input: { subject?: string; csrf?: string; connected?: boolean; loginCsrf?: string } = {}) {
  const form = (path: string, label: string) => `<form method="post" action="${path}"><input type="hidden" name="csrf" value="${escape(input.csrf ?? '')}"><button>${label}</button></form>`;
  return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Account access</title><style>${accountStyles}</style><main><h1>Account access</h1>${input.subject ? `<p>Signed in as ${escape(input.subject)}</p><p>${input.connected ? 'Repository access connected.' : 'Repository access is not connected.'}</p><p>GitHub’s repository permission is broad: it can allow upstream writes across repositories you can access. This service permits only reads and keeps credentials on the server.</p>${form('/account/repositories/connect', 'Connect repository access')}${form('/account/disconnect', 'Disconnect repository access')}${form('/account/signout', 'Sign out')}<p>Disconnect revokes this service’s repository access. Sign out ends this browser session.</p>` : input.loginCsrf ? `<form method="post" action="/account/signin"><input type="hidden" name="csrf" value="${escape(input.loginCsrf)}"><button>Sign in with GitHub</button></form>` : '<p>Sign-in is currently unavailable while the secure provider connection is completed.</p><button disabled>Sign in with GitHub</button>'}<p><a href="/account/public">Continue with public access</a></p></main></html>`;
}
export function browserHeaders() { return { 'Cache-Control': 'private, no-store', 'CDN-Cache-Control': 'no-store', 'Cloudflare-CDN-Cache-Control': 'no-store', 'Content-Security-Policy': "default-src 'none'; style-src 'unsafe-inline'; form-action 'self'; frame-ancestors 'none'; base-uri 'none'", 'Referrer-Policy': 'no-referrer', 'X-Content-Type-Options': 'nosniff' }; }
